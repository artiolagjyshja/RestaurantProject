<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Category for Restaurant App -->
        <record model="ir.module.category" id="module_category_restaurant">
            <field name="name">Restaurant Management</field>
            <field name="description">Manage access to Restaurant models</field>
            <field name="sequence">10</field>
        </record>

        <!-- Group: Waiter -->
        <record id="group_restaurant_waiter" model="res.groups">
            <field name="name">Waiter</field>
            <field name="category_id" ref="module_category_restaurant"/>
        </record>

        <!-- Group: Manager -->
        <record id="group_restaurant_manager" model="res.groups">
            <field name="name">Manager</field>
            <field name="category_id" ref="module_category_restaurant"/>
            <field name="implied_ids" eval="[(4, ref('group_restaurant_waiter'))]"/>
            <field name="users" eval="[(4, ref('base.user_root')), (4, ref('base.user_admin'))]"/>
        </record>

    </data>
</odoo>
